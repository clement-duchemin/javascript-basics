/* let direction = 'haut';
let position = {x:0, y:0};


switch(direction) {
    case 'haut':
        position.y += 1;
        break;
};

console.log(`New position is (${position.x}, ${position.y}).`) */