/* let tasks = [];

let action = prompt("Voulez-vous afficher, supprimer ou ajouter des tâches ?");

if(action === 'ajouter') {
    newTask = prompt('Ajouter la nouvelle tâche:');
    newTaskDisplayed = alert(newTask)
    tasks.push(newTask);
    alert("Tâche ajoutée !");
}else if(action === 'supprimer') {
    deletedTask = alert('tâche supprimée ');
    tasks.splice(index, 1);
}else if(action === 'afficher') {
    taskDisplayed = alert('tâche affiché');
} */