/* let intNumber = parseInt(prompt("Entrez un entier positif"), 10);

let count = 0;

while(intNumber != 1) {
    if(intNumber % 2 === 0) {
        intNumber = intNumber/2;
    }
    else {
        intNumber = (intNumber*3) + 1;
    };
    count++;
    console.log(intNumber)
}
console.log(`Itérations:${count}`); */