/* let nombreSecret = Math.floor(Math.random()*100)+1;

let guess = prompt("Devinez le nombre entre 1 et 100.");

while(guess != nombreSecret) {
    if(guess < nombreSecret) {
        alert("trop bas.")
    } else if(guess > nombreSecret) {
        alert("Trop haut.")
    }

     guess = prompt("Devinez le nombre entre 1 et 100.");

}

alert("bravo"); */